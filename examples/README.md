# examples

这里放仓库级示例与模板，不放主入口说明。

## 内容分类

- 任务样例：`任务样例-*.md`
- 协议流样例：`json/*.json`
- 状态样例：queue / log / memory / capability / scorecard / final brief
- 测试模板：`tests/`

## 使用原则

- 示例用于说明协议、状态结构、测试输入输出。
- 生成产物应放到 `examples/generated/`，且通常不应提交到仓库。
- 当 schema、脚本或协议更新时，这里的示例也要同步更新。
